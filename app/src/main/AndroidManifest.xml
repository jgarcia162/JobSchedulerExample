<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.jobschedulerexample">
    

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--we can register our JobService statically through the manifest. Then specify the Android permission to Bind a service-->
        <service android:name=".TestJobService"
            android:label="Headset Plugged In Service"
            android:permission="android.permission.BIND_JOB_SERVICE"/>

        <!--<receiver android:name=".StartServiceReceiver"-->
            <!--android:exported="true">-->
            <!--<intent-filter>-->
                <!--&lt;!&ndash;this intent filter is the action our broadcast receiver in our app is listening for. When a headset is plugged in, our StartServiceReceiver will execute its onReceive method which calls JobUtil.scheduleJob() which then schedules the Job we asked to be scheduled&ndash;&gt;-->
                <!--<action android:name="android.intent.action.HEADSET_PLUG"/>-->
            <!--</intent-filter>-->
        <!--</receiver>-->

    </application>

</manifest>